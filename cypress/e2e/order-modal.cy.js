/* eslint-disable jest/expect-expect */
describe("order modal test", () => {
  beforeEach(() => {
    cy.prepareWithGetUser();
  });

  it("open order modal", () => {
    cy.prepareWithGetUser();
  });
});
